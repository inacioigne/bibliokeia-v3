"use client";
// MUI
import {
  AppBar,
  Divider,
  Box,
  Toolbar,
  IconButton,
  MenuList,
  MenuItem,
  ListItemIcon,
  ListItemText,
  Typography,
  Button,
} from "@mui/material/";

// Providers BiblioKeia
import { ProgressProvider, useProgress } from "src/providers/progress";

export default function Dashboard() {
  // const { progress, setProgress } = useProgress();

  return <Box>Dashboard Content</Box>;
}
